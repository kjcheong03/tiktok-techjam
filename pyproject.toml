[project]
name = "ghostlab-baselines"
version = "0.1.0"
description = "Reproducible retrieval and conversation-state baselines for TechJam Track 4"
requires-python = ">=3.10,<3.14"
dependencies = [
  "numpy>=1.26,<3",
  "sentence-transformers>=6,<7",
]

[tool.uv]
package = false
